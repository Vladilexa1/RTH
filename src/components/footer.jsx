export default function Footer() {
    return (
        <nav class="footer bg-blue">
        <div class="container-fluid">
          <button class="btn btn-primary">SAVE</button>
        </div>
      </nav>
    );
}